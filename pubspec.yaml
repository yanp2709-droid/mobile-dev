name: first_application_Yan
description: "A new Flutter project."

publish_to: "none"
version: 0.1.0+1
environment:
  sdk: ^3.9.0
dependencies:
  flutter:
    sdk: flutter
  english_words: ^4.0.0
  provider: ^6.1.5
  firebase_core: ^4.7.0
  firebase_auth: ^6.4.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
flutter:
  uses-material-design: true